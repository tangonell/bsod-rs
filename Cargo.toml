[package]
name = "bsod-rs"
version = "0.1.0"
edition = "2024"
license = "0BSD"

[profile.dev]
panic = "abort"

[profile.release]
lto = true
opt-level = "z"
codegen-units = 1
panic = "abort"
strip = true